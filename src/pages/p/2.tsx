import { PageProps } from "gatsby";
import React, { FC } from "react";

const ProductDetail2: FC<PageProps> = (props) => {
  console.log(props);

  return <div>ProductDetail 2</div>;
};

export default ProductDetail2;
